export interface TranslationData {
  languageFrom: string; // Язык исходного текста
  languageTo: string;   // Язык перевода
  textFrom: string;   // Текст до перевода
  textTo: string;    // Текст после перевода
}